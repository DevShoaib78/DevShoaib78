<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 86" width="900" height="86" role="img" aria-label="Note: most repositories here are private because they hold client code. The work itself is public, every project above is live and the links go straight to it.">
  <style>
    .lead { font:italic 700 19px 'Segoe UI','Helvetica Neue',Helvetica,Arial,sans-serif; fill:#fca5a5; }
    .body { font:italic 400 19px 'Segoe UI','Helvetica Neue',Helvetica,Arial,sans-serif; fill:#fca5a5; }
    .fade { opacity:0; animation:fade .8s ease-out .2s forwards; }
    @keyframes fade { to { opacity:1; } }
  </style>
  <text class="fade" x="450" y="34" text-anchor="middle">
    <tspan class="lead">Note:</tspan><tspan class="body" dx="8">Most repositories here are private, because they hold client code.</tspan>
  </text>
  <text class="body fade" x="450" y="64" text-anchor="middle">The work itself is public: every project above is live, and the links go straight to it.</text>
</svg>
